# Core stack — enough for every notebook in machina_learning/
# Install with:  pip install -r requirements.txt
# Tested on Python 3.10–3.12.

numpy>=1.26
scipy>=1.11
pandas>=2.0
matplotlib>=3.8
seaborn>=0.13
scikit-learn>=1.4
xgboost>=2.0

# Notebook environment
jupyterlab>=4.0
ipykernel>=6.29
ipywidgets>=8.1

# Tree visualisation in "lab - XGBoost and Gradient Boosting"
# NOTE: the Python binding needs the Graphviz system package too:
#   Ubuntu/Debian: sudo apt install graphviz
#   macOS:         brew install graphviz
graphviz>=0.20
